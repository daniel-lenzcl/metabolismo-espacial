using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rede : MonoBehaviour
{
    private GameObject centro;
    private List<GameObject> contato;

  //  public LineRenderer linhaContato;

    // Start is called before the first frame update
    void Start()
    {
       // centro = this.parent;
        //contato = this.GetComponent<conectados>().other;
  //      linhaContato = this.GetComponent<LineRenderer>();

    }

    // Update is called once per frame
    void Update()
    {
    //    linhaContato.SetPosition(0, this.transform.position);
    //    linhaContato.SetPosition(1, c.transform.position);

    }
}
